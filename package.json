{
  "name": "monzo-monitoring-app",
  "version": "1.5.3",
  "description": "MonzoMonitoringApp is a dockerised web app for monitoring and managing your Monzo bank account.",
  "repository": "git@github.com:whale-watching/monzo-monitoring-app.git",
  "author": "Chris Chen <ChrisChen@users.noreply.github.com>",
  "private": false,
  "scripts": {
    "start": "docker compose --env-file .env -f docker-compose.dev.yml up -d --build",
    "stop": "docker compose --env-file .env -f docker-compose.dev.yml down",
    "logs": "docker compose -f docker-compose.dev.yml logs -f"
  }
}
